# Integration evidence output

`tests/integration/run-real-runtime.sh` writes timestamped, sanitized JSON and logs here. Generated evidence is intentionally ignored. It must contain commands, service/network assertions, revisions, and outcomes—never environment values, credentials, database dumps, evidence payloads, or permit key material.
