# Connector capability records

These manifests are release claims, not marketing inventory. `hidden` means the connector must not appear as selectable in the UI. `fixture_tested` is not live certification, and `live_tested` requires a named environment/version and evidence path. Every operation retains `controller_required: true`, including local artifact imports, so scope, size, concurrency, audit, and workspace policy remain centralized.

