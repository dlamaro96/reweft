# Target capability records

The canonical modernization specification remains platform-neutral. These records describe only translation and validation coverage. `generated`, `schema_validated`, `fixture_tested`, and `environment_tested` are separate states; an adapter cannot advance without corresponding evidence.

